# 동아리 활동 기록 관리 웹앱

동아리 임원이 활동 내역을 등록·조회·삭제할 수 있는 웹앱입니다.
서버·DB 없이 브라우저 localStorage만으로 동작합니다.

## 팀원 명단과 역할

| 이름 | 역할 |
|---|---|
| 임채호 | 필수기능 담당 (`app.js`) — 활동 등록, 목록 조회, 활동 삭제 |
| 조영남 | 선택기능 담당 (`features.js`) — 키워드 검색, JSON 내보내기/가져오기, README 초안, 검증 로직 리뷰 |

## 구현한 기능

### 필수 기능
- [ ] 활동 등록 (활동명, 날짜, 장소, 참여 인원, 메모 입력·저장)
- [ ] 활동 목록 조회 (최신순 표시, 빈 목록일 때 안내 문구)
- [ ] 활동 삭제 (삭제 전 확인 절차)
- [ ] 입력값 검증 (활동명 필수 / 날짜 미래 불가 / 참여 인원 1 이상 정수)

### 선택 기능
- [ ] 활동명·장소 키워드 검색 (입력 즉시 실시간 필터링)
- [ ] 데이터 JSON 내보내기 / 가져오기 (가져오기는 기존 데이터에 추가로 합침, id 중복 시 새 id 부여)

## 실행 방법

1. 이 저장소를 clone 또는 다운로드한다.
2. `index.html` 파일을 브라우저로 연다. (별도 설치·서버 실행 불필요)

## 데이터 구조

활동 1건은 다음 형태로 `localStorage`의 `"activities"` 키에 배열로 저장됩니다.

```json
{ "id": 1, "title": "정기모임", "date": "2026-08-01", "place": "학생회관 201호",
  "memberCount": 12, "memo": "신입생 환영", "createdAt": "2026-08-01T14:00:00" }
```

## 파일 구조

- `index.html` : 화면 구조
- `style.css` : 스타일
- `app.js` : 필수 기능 로직 (등록/조회/삭제)
- `features.js` : 선택 기능 로직 (검색, JSON 내보내기/가져오기)
- `CLAUDE.md` : 개발 규칙 및 진행 상황

## 실행 화면

<!-- 구현 완료 후 스크린샷 2장 이상 추가 (예: 활동 등록 화면, 목록/검색 화면) -->

---
> 이 파일은 `README.md`와 내용이 동일한 팀 내부 협업용 사본입니다. 제출용 파일명은 `README.md`이니, 내용을 수정할 땐 두 파일을 함께 갱신하세요.

소감
조영남: AI기능을 활용하여 팀원과 프로젝트를 진행하는 경험은 처음인데 확실히 장단점이 있으면서도 쓸수있다면 활용성이 높다는 것을 채감하였습니다.
프로젝트 완료 후 개인적으로 좀더 신경쓰이는 디테일을 마져보고싶은 마음이 생기네요.
