# 프로젝트 개요
동아리 활동 기록 관리 웹앱.
동아리 임원이 활동 내역을 등록하고 조회·삭제할 수 있다.

팀: 코드 한입 조 (팀번호 없음)
팀원: 임채호, 조영남

# 기술 스택
- HTML / CSS / JavaScript (프레임워크 없음)
- 데이터 저장: 브라우저 localStorage
- 외부 라이브러리: 없음

# 파일 구조
- index.html : 화면 구조
- style.css  : 스타일
- app.js     : 필수 기능 로직 (등록/조회/삭제) — 임채호 담당
- features.js: 선택 기능 로직 (검색, JSON 내보내기/가져오기) — 조영남 담당
(index.html에서 app.js, features.js 둘 다 불러온다. 구조를 바꾸면 이 항목도 함께 수정한다)

# 코딩 규칙
- 함수와 변수 이름은 영어 camelCase, 의미가 드러나게 작성
- 주요 함수 위에는 한 줄 한글 주석을 단다
- 하나의 함수는 하나의 일만 한다
- 들여쓰기는 스페이스 2칸

# 데이터 구조
활동 1건은 다음 형태로 저장한다.
{ id, title, date, place, memberCount, memo, createdAt }
localStorage 키: "activities"

# 협업 방식 (참고)
- 서버·DB가 없어 localStorage는 기기(브라우저)별로 분리된다.
- 여러 임원이 함께 쓸 때는 같은 기기를 공유해서 사용하거나,
  "JSON 내보내기/가져오기" 기능으로 데이터를 파일로 옮겨서 합친다.
- 로그인/인증 기능은 만들지 않는다 (서버 없이는 실질적 보안 의미가 없고, 과제 범위에도 없음).

# 선택 기능 상세 동작
- **키워드 검색**: 입력창에 타이핑하는 즉시 실시간으로 목록 필터링 (검색 버튼 없음). 활동명·장소 대상.
- **JSON 가져오기**: 기존 localStorage 데이터에 추가로 합친다 (덮어쓰지 않음). 불러온 항목의 id가 기존 항목과 겹치면 새 id를 부여해 충돌을 피한다.

# 팀원 역할 분담 (2인, 동시 작업 가능하도록 결합도 기준으로 분리)

## 0단계 (공동, 시작 시 ~5분)
아래 인터페이스(함수 이름·입출력)만 먼저 합의하고 각자 갈라져서 작업한다.
- `getActivities()` : localStorage에서 활동 배열을 읽어 반환
- `saveActivities(list)` : 활동 배열을 localStorage에 저장
- `renderActivities(list)` : 배열을 받아 목록 화면을 그림 (검색 결과도 이 함수를 그대로 재사용)
- index.html에 각자 영역의 id를 미리 정해둔다 (예: `#register-section`, `#list-section`, `#search-section`, `#io-section`)

## 담당 (병렬 작업)
- **임채호** (`app.js`): 필수기능 3개 전부 — 활동 등록(폼+저장+검증 3종), 목록 조회(최신순 렌더링, 빈 목록 안내), 활동 삭제(확인 절차)
  - 등록·조회·삭제는 모두 같은 렌더링 함수를 공유해 강하게 결합되어 있으므로 한 사람이 처음부터 끝까지 담당한다.
- **조영남** (`features.js`): 선택기능 2개 — 키워드 검색(실시간 필터), JSON 내보내기/가져오기
  - `getActivities()`/`renderActivities()` 인터페이스만 맞으면 임채호의 실제 구현이 끝나기 전에도 가짜 데이터로 독립 개발 가능
  - 추가로 README 초안, 입력값 검증 리뷰 담당

15:00경 중간 지점에 두 파일을 index.html에서 함께 불러와 통합 확인. 마무리(테스트·README·보고서)는 공동 작업.

# 작업 방식
- 코드를 작성하기 전에 무엇을 어떤 순서로 만들지 먼저 제안한다
- 한 번에 하나의 기능만 구현한다. 여러 기능을 동시에 만들지 않는다
- 기존에 동작하던 코드를 요청 없이 수정하거나 삭제하지 않는다
- 확실하지 않은 부분은 추측하지 말고 먼저 질문한다

# 하지 말 것
- 서버, 데이터베이스, 빌드 도구 도입 금지
- 요청하지 않은 파일 새로 생성 금지
- 사용하지 않는 예시 코드나 더미 데이터 대량 생성 금지
- 한 번에 100줄 넘는 코드를 통째로 출력하지 않기
- 이미지/파일 업로드 기능 구현 금지 (localStorage 용량 한계 + 과제 범위 밖)
- 로그인/회원가입 기능 구현 금지 (과제 범위 밖, 서버 없이는 무의미)

# 커밋 규칙
- 기능 하나가 동작하면 바로 커밋한다
- 커밋 메시지 형식: <기능명>: <무엇을 했는지>
  예) 활동등록: 입력 폼과 저장 기능 구현

# 현재 진행 상황
- [ ] 활동 등록
- [ ] 활동 목록 조회
- [ ] 활동 삭제
- [ ] 입력값 검증 (활동명 필수 / 날짜는 미래 불가 / 참여인원 1 이상 정수)
- [x] 선택 기능: 활동명·장소 키워드 검색
- [x] 선택 기능: 데이터 JSON 내보내기/가져오기
