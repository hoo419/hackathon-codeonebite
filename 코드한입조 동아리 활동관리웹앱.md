# 코드한입조 동아리 활동관리웹앱 — 작업 요약

- 작성일시: 2026-08-05

## 과제 개요
해커톤 과제 "동아리 활동 기록 관리 웹앱". 기술 조건은 HTML/CSS/JS + 브라우저 localStorage만 사용, 서버·DB·빌드 도구 금지.
팀: 코드 한입 조 (팀번호 없음) / 팀원: 임채호, 조영남

## 논의 및 결정 사항
1. **기술 제약 확인**: 서버/DB/빌드도구 금지 → 데이터는 localStorage에만 저장.
2. **"임원진 공동 관리" 방식 재정의**: 로그인 기능은 만들지 않기로 결정.
   - localStorage는 기기(브라우저)별로 분리되어 있어 로그인해도 여러 임원 간 데이터가 자동으로 섞이지 않음.
   - 서버 없이 저장하는 비밀번호는 개발자도구로 그대로 노출되어 실질적 보안 의미가 없음.
   - 과제의 필수/선택 기능 목록에도 로그인은 없음.
   - → 대신 같은 기기를 여러 임원이 함께 쓰거나, JSON 내보내기/가져오기로 수동 동기화하는 방식으로 대체.
3. **이미지/파일 업로드 검토 후 제외**: localStorage 용량(보통 5~10MB) 대비 이미지의 Base64 인코딩 크기가 너무 커서 금방 용량 초과가 남. 기기 간 공유도 안 됨.
   - 대안으로 "파일저장소 링크(URL)만 텍스트로 기록"하는 방식은 기술적으로 문제없음을 확인했으나(용량 부담 없고 실제 파일은 외부에서 공유됨), 시간 대비 우선순위상 이번 선택 기능 범위에는 넣지 않기로 함.
4. **선택 기능 범위 확정 (2개)**:
   - ① 활동명·장소 키워드 검색
   - ② 데이터 JSON 내보내기/가져오기
5. `CLAUDE.md` 작성 완료.

## 확정된 스펙 요약
- **필수 기능**: 활동 등록 / 목록 조회(최신순, 빈 목록 안내) / 삭제(확인 절차 포함)
- **입력 검증**: 활동명 필수, 날짜는 미래 불가, 참여 인원은 1 이상 정수
- **데이터 구조**: `{ id, title, date, place, memberCount, memo, createdAt }`, localStorage 키 `"activities"`
- **선택 기능**: 키워드 검색, JSON 내보내기/가져오기
- **파일 구조**: `index.html`, `style.css`, `app.js`
- **하지 않는 것**: 로그인/회원가입, 이미지·파일 업로드, 서버/DB/빌드 도구 도입

## 선택 기능 상세 동작 (확정)
- **키워드 검색**: 입력 즉시 실시간 필터링 (버튼 없음), 활동명·장소 대상
- **JSON 가져오기**: 기존 데이터에 추가로 합침 (덮어쓰지 않음), id 중복 시 새 id 부여

## 팀원 역할 분담 (2인, 진짜 동시 작업 가능하도록 재설계)
처음엔 "임채호=등록+조회 / 조영남=삭제+선택기능"으로 나눴으나, 삭제 기능이 목록 렌더링(임채호 담당)에 강하게 결합돼 있어 조영남이 기다려야 하는 문제를 발견 → **결합도 기준으로 재분배**.

- **0단계 (공동, ~5분)**: 인터페이스만 먼저 합의
  - `getActivities()` / `saveActivities(list)` / `renderActivities(list)` 함수 이름·입출력 확정
  - index.html의 영역별 id 미리 정의 (`#register-section`, `#list-section`, `#search-section`, `#io-section`)
  - JS 파일도 2개로 분리: `app.js`(임채호) / `features.js`(조영남) — 같은 파일 동시 수정으로 인한 merge 충돌 방지
- **임채호** (`app.js`): 필수기능 3개 전부 — 등록/조회/삭제 (서로 강결합이라 한 사람이 통으로 담당)
- **조영남** (`features.js`): 선택기능 2개 — 키워드 검색(실시간), JSON 내보내기/가져오기 (약결합, 인터페이스만 맞으면 독립 개발 가능) + README 초안 + 검증 로직 리뷰
- 15:00경 두 파일 통합 확인, 마무리(테스트·README·보고서)는 공동 작업

## 다음 단계
1. `README.md` 작성 여부 결정 (팀원 명단·역할, 구현 기능, 실행 방법, 스크린샷 포함 필수)
2. 구현 시작 전 계획 수립 → 화면 캡처 (보고서 3-2 항목 제출용)
3. `index.html` → `style.css` → `app.js` 순서로 구현 (기능 단위로 하나씩)
4. 기능 하나 완성될 때마다 커밋
5. 보고서 PDF 작성 (`03_보고서_양식.md` 참고, 프롬프트 기록·오류 해결 사례 포함)

## 관련 파일
- 요구사항 명세서: `../새 폴더/01_요구사항_명세서.md`
- CLAUDE.md 작성 템플릿: `../새 폴더/02_CLAUDE_템플릿.md`
- 보고서 양식: `../새 폴더/03_보고서_양식.md`
- 이 프로젝트 CLAUDE.md: `CLAUDE.md`
